import { MultiplyStringPipe } from './multiply-string.pipe';

describe('MultiplyStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyStringPipe();
    expect(pipe).toBeTruthy();
  });
});
